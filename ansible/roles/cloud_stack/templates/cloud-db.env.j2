# Managed by Ansible (roles/cloud_stack) — postgres container init.
# Single role (owner = app) taken from the vritti-cloud production env; migrations create `cloud`.
POSTGRES_USER={{ _db_user }}
POSTGRES_PASSWORD={{ _db_pass }}
POSTGRES_DB={{ _db_name }}
PGDATA=/var/lib/postgresql/data/pgdata
